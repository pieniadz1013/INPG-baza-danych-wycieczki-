// funkcja znajduje wartość największą (według ceny)

struct drzewo* max(struct drzewo* korzen)
{
	if(korzen == NULL)
		printf("Drzewo jest puste");
	if(korzen->prawy==NULL)
	{
		return korzen;
	}
	else
	{
		max(Wezel->prawy);
	}
}
