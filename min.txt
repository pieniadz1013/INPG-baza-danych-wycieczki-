/*funkcja znajduje najmniejszą wartość na liście*/

Wezel* MIN(Wezel *korzen)
{
  if(korzen == NULL)
	printf("Drzewo jest puste");
  else
  {
	if(korzen->lewy == NULL)
        return korzen;
	else
        MIN(korzen->lewy);
  }
}
