/*funkcja znajduje najmniejszą wartość na liście*/

struct drzewo *MIN(struct Wezel *korzen)
{
  if(korzen == NULL)
	printf("Drzewo jest puste");
  else
  {
	if(korzen->lewy == NULL)
        return korzen;
	else
        MIN(korzen->lewy);
  }
}
